<UserControl x:Class="BindingDemoMVVM.Controls.OneWayBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="{Binding DataContext.LocalizationManager[OneWay_Title], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding DataContext.LocalizationManager[OneWay_Description], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"
                   TextWrapping="Wrap" Margin="0,0,0,15"/>
        <TextBlock Text="{Binding DataContext.LocalizationManager[OneWay_CurrentTimeLabel], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Margin="0,0,0,4"/>
        <TextBlock Text="{Binding CurrentTime, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
    </StackPanel>
</UserControl>
