<UserControl x:Class="BindingDemoMVVM.Controls.DefaultBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BindingDemoMVVM.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="Привязка по умолчанию (Mode не указан)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="Для TextBox по умолчанию TwoWay, для TextBlock/Slider/CheckBox — OneWay." 
                   TextWrapping="Wrap" Margin="0,0,0,15"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Текст:" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Text}" Margin="0,5"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Число:" VerticalAlignment="Center" Margin="0,5"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Number}" Margin="0,5"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Флаг:" VerticalAlignment="Center" Margin="0,5"/>
            <CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding Flag}" Content="Включено" Margin="0,5" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Слайдер:" VerticalAlignment="Center" Margin="0,5"/>
            <Slider Grid.Row="3" Grid.Column="1" Value="{Binding Value}" Minimum="0" Maximum="1" Margin="0,5"/>
        </Grid>
    </StackPanel>
</UserControl>
