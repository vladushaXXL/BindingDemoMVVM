<UserControl x:Class="BindingDemoMVVM.Controls.TwoWayBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="{Binding DataContext.LocalizationManager[TwoWay_Title], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding DataContext.LocalizationManager[TwoWay_NameLabel], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Margin="0,0,0,4"/>
        <TextBox Text="{Binding UserName, Mode=TwoWay}" Margin="0,0,0,8"/>
        <TextBlock Text="{Binding DataContext.LocalizationManager[TwoWay_MirrorLabel], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Margin="0,0,0,4"/>
        <TextBlock Text="{Binding UserName, Mode=OneWay}" FontStyle="Italic" Margin="0,0,0,15"/>
        <TextBlock Text="{Binding DataContext.LocalizationManager[TwoWay_AgeLabel], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" Margin="0,0,0,4"/>
        <Slider Value="{Binding Age}" Minimum="0" Maximum="120" Margin="0,0,0,4"/>
        <TextBlock Margin="0,0,0,15">
            <Run Text="{Binding DataContext.LocalizationManager[TwoWay_AgeValuePrefix], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"/>
            <Run Text="{Binding Age}"/>
            <Run Text="{Binding DataContext.LocalizationManager[TwoWay_AgeValueSuffix], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"/>
        </TextBlock>
        <CheckBox Content="{Binding DataContext.LocalizationManager[TwoWay_IsActiveLabel], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}" IsChecked="{Binding IsActive}" Margin="0,0,0,4"/>
        <TextBlock FontStyle="Italic">
            <Run Text="{Binding DataContext.LocalizationManager[TwoWay_IsActiveValuePrefix], RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"/>
            <Run Text="{Binding IsActive}"/>
        </TextBlock>
    </StackPanel>
</UserControl>
