<UserControl x:Class="BindingDemoMVVM.Controls.OneTimeBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BindingDemoMVVM.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="Одноразовая привязка (OneTime)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="Значение передаётся только при загрузке. Изменения в ViewModel после загрузки не отображаются." 
                   TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBlock Text="TextBox привязан к InitialValue с Mode=OneTime:" Margin="0,0,0,4"/>
        <TextBox Text="{Binding InitialValue, Mode=OneTime}" Margin="0,0,0,15"/>
        <TextBlock Text="При вводе в TextBox значение в UI не обновляется из ViewModel (OneTime уже выполнен). 
Изменение в коде InitialValue после загрузки тоже не попадёт на экран." 
                   TextWrapping="Wrap" Foreground="Gray" FontSize="11"/>
    </StackPanel>
</UserControl>
